# 用户角色管理指南

本文档介绍如何在学生信息管理系统中管理用户角色和权限。

## 📋 角色类型

| 角色 | 代码 | 中文名称 | 权限范围 |
|------|------|----------|----------|
| `admin` | 管理员 | 系统管理员 | 所有功能（学生管理 + 考试管理 + 成绩管理 + 成绩分析 + 系统管理） |
| `grade_manager` | 级长 | 年级主任/级长 | 教学管理（学生管理 + 考试管理 + 成绩管理 + 成绩分析） |
| `subject_teacher` | 科任老师 | 任课教师 | 基础功能（学生管理） |
| `staff` | 教辅人员 | 教辅工作人员 | 基础功能（学生管理） |

## 🛠️ 管理方式

### 方法一：Django Admin 后台管理（推荐）

#### 1. 访问后台
```
浏览器访问：http://127.0.0.1:8000/admin/
使用超级用户账号登录
```

#### 2. 用户管理操作
- 点击 "用户" 进入用户列表
- 选择要编辑的用户
- 在 "角色信息" 部分设置：
  - **角色**：从下拉菜单选择角色
  - **负责年级**：如果是级长，填写负责的年级（如：高一、高二、初三等）

### 方法二：命令行管理

#### 1. 设置单个用户角色

```bash
# 基本语法
python3 manage.py set_user_role <用户名> <角色> [--grade=年级]

# 示例：设置用户为管理员
python3 manage.py set_user_role admin admin

# 示例：设置用户为级长并指定负责年级
python3 manage.py set_user_role zhangsan grade_manager --grade="高一"

# 示例：设置用户为科任老师
python3 manage.py set_user_role lisi subject_teacher

# 示例：设置用户为教辅人员
python3 manage.py set_user_role wangwu staff
```

#### 2. 批量创建演示用户

```bash
# 创建预设的演示用户（包含各种角色）
python3 manage.py create_demo_users
```

演示用户包括：
- `teacher001` / `teacher123` - 科任老师
- `manager001` / `manager123` - 级长（负责高一）
- `staff001` / `staff123` - 教辅人员

## 📝 实际操作示例

### 新学期用户设置流程

#### 1. 创建管理员
```bash
# 先创建超级用户（如果还没有）
python3 manage.py createsuperuser

# 或者将现有用户设为管理员
python3 manage.py set_user_role existing_user admin
```

#### 2. 创建级长用户
```bash
# 高一级长
python3 manage.py set_user_role gaoyi_manager grade_manager --grade="高一"

# 高二级长  
python3 manage.py set_user_role gaoer_manager grade_manager --grade="高二"

# 高三级长
python3 manage.py set_user_role gaosan_manager grade_manager --grade="高三"
```

#### 3. 创建科任老师
```bash
# 语文老师
python3 manage.py set_user_role yuwen_teacher subject_teacher

# 数学老师
python3 manage.py set_user_role shuxue_teacher subject_teacher

# 英语老师
python3 manage.py set_user_role yingyu_teacher subject_teacher
```

#### 4. 创建教辅人员
```bash
# 教务助理
python3 manage.py set_user_role jiaowu_staff staff

# 档案管理员
python3 manage.py set_user_role dangan_staff staff
```

## 🔐 权限验证

### 测试角色权限
1. 用不同角色的用户登录前端：`http://localhost:3000/login`
2. 查看首页显示的功能模块：
   - **管理员**：看到所有模块
   - **级长**：看到教学相关模块
   - **科任老师/教辅人员**：只看到学生管理

### 功能模块对应链接
- 学生管理：`http://127.0.0.1:8000/students/`
- 考试管理：`http://127.0.0.1:8000/exams/`
- 成绩管理：`http://127.0.0.1:8000/scores/`
- 成绩分析：`http://127.0.0.1:8000/analysis/class-grade/`
- 系统管理：`http://127.0.0.1:8000/admin/`

## 🚨 注意事项

1. **用户名唯一性**：确保用户名不重复
2. **密码安全**：为实际用户设置强密码
3. **年级格式**：级长的负责年级建议使用统一格式（如：高一、高二、初三等）
4. **权限测试**：创建用户后务必测试登录和功能访问
5. **备份重要**：在生产环境中修改用户角色前请备份数据

## 🔄 常见操作

### 查看所有用户
访问 Django Admin：`http://127.0.0.1:8000/admin/` → 用户

### 批量导入用户
如需批量导入，可修改 `create_demo_users.py` 中的 `users_data` 列表，然后运行：
```bash
python3 manage.py create_demo_users
```

### 重置用户角色
```bash
# 将用户角色重置为默认的教辅人员
python3 manage.py set_user_role username staff
```

---

📞 **技术支持**：如有问题，请检查 Django 日志或联系系统管理员。